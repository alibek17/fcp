---
- name: Install Apache WebServer
  when: ansible_distribution == "Ubuntu"
  hosts: all
  become: yes
  become_method: sudo
  tasks: 
    - name: install apache
      apt:
        name: apache2
        state: present
      when: ansible_distribution == "Ubuntu"
